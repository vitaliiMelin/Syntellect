const getDayCounter = (fullDistance: number) => {
  let days = 0
  do {
    fullDistance -= 50
    days++
    if (fullDistance <= 0) break
    fullDistance += 30
  } while (fullDistance > 0)
  return days
}

console.log(getDayCounter(100))

Ответ: 4
